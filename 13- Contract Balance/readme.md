# Contract Balance

This folder demonstrates how a contract can receive and send Ether.

## Code Overview

- `payEther()` is `payable` so the contract can receive ETH.
- `getBalance()` returns the contract's current balance.
- `sendEtherAccount()` transfers 1 ether to a fixed payable address.

## Files

- `demo.sol`: contract balance and transfer example.
